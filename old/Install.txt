


//Docker commands
docker ps
docker ps -a
docker rm ..
docker rmi ..
docker volume ls

docker exec -it  screeps /bin/bash
docker exec -it  screeps npx screeps cli

map.generateMap({start: 'W10N10', end: 'E10S10', closed: { left: false }, borders: { left: 2 }})



install

init
run
docker ps -a
docker exec -it 3d9a120e5e16 /bin/bash
yarn add screepsmod-auth screepsmod-mongo

npm install screeps-mod-mapgen

https://github.com/jkingry/screeps-mod-mapgen



{
  "mods": [
    "node_modules/screepsmod-auth/index.js",
    "node_modules/screepsmod-tickrate/index.js",
    "node_modules/screepsmod-admin-utils/index.js",
    "node_modules/screepsmod-features/index.js"
  ],
  "bots": {
    "simplebot": "node_modules/@screeps/simplebot/src"
  }
}
mongo.importDB()
system.resetAllData()
map.generateMap({start: 'W10N10', end: 'E10S10', closed: { left: false }, borders: { left: 2 }})
utils.removeBots()
setPassword('vsolo0', 'v12345')

npm rebuild //if problem

cat /etc/*release*

sudo apt-get purge mongodb mongodb-server mongodb-server-core mongodb-clients
sudo apt-get purge mongodb-org
sudo apt-get autoremove

sudo apt-get update
sudo apt-get install mongodb-org
---
sudo dpkg --remove --force-remove-reinstreq mongo-tools
sudo dpkg --remove --force-remove-reinstreq mongodb-server-core

env
/etc/init.d/dbus start

systemctl start mongod
systemctl daemon-reload
systemctl status mongod
mongod --version

# mkdir /data/db
~# mkdir /data/db/log

~# mongod --dbpath data/db --fork --logpath data/db/log

mongod --shutdown

mongod --fork --logpath /data/db/log/mongodb.log

service mongodb start


//redis
1- Comment out bind 127.0.0.1

2- Change protected-mode to no

redis-cli config get dir

docker network create -d bridge --subnet 192.168.0.0/24 --gateway 192.168.0.1 dockernet

apt-get update
apt-get install net-tools


docker-compose build
docker-compose up
